

function setColours(colours) {
    State.ColourState.colours = colours;
}

function setColour(colour) {
    State.ColourState.colour = colour;
}

function addColour(colour) {
    State.ColourState.colours.push(colour);
}